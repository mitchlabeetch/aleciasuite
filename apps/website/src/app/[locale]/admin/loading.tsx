import { AdminSkeleton } from "@/components/admin/AdminSkeleton";

/**
 * Admin Root Loading State
 *
 * Displayed while the admin layout or page is loading.
 */
export default function AdminLoading() {
	return <AdminSkeleton />;
}
